1.排序元素个数市正整数;
2.只讨论基于比较的排序;
3.只讨论内部排序:指可以全部在内存中处理的排序,如果只有2GB的内存,要排序10GB的数据,是不可能全部在内存中完成的，这种叫外部排序，这里不讨论这种情况;
4.稳定性:任意两个相等的数据,排序前后的相对位置不发生变化;
5.没有一种排序是任何情况下都表现最好的;每一种算法都有其存在的理由。每一种算法都是在数据存在某种特征时候最好的，当特征变化时，就可能需要其他的算法了。